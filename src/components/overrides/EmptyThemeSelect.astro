{
	/* Intentionally renders nothing — hides the light/dark toggle since the site is
	light-only for now. Delete this override (and its entry in astro.config.mjs's
	`components` map) to bring back Starlight's default ThemeSelect once a real
	dark palette is designed. */
}
